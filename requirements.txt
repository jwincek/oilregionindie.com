# Core — Django 5.2 LTS (security support through April 2028).
Django>=5.2,<5.3
wagtail>=7.3,<7.4
Pillow>=10.0

# Authentication
django-allauth>=65.0
django-axes>=7.0

# Audit history (UserProfile, Report, CreatorProfile, VenueProfile, BookingRequest)
django-simple-history>=3.0

# Frontend interactivity
django-htmx>=1.28.0

# Configuration
django-environ>=0.14.0

# Payments
stripe>=10.0

# Storage
django-storages>=1.14
boto3>=1.43.51

# Database
psycopg[binary]>=3.2

# Task queue
django-q2>=1.10.0

# Cache & sessions
django-redis>=5.4

# Production server
gunicorn>=23.0

# Error monitoring (only active when SENTRY_DSN is set in .env)
sentry-sdk[django]>=2.66.0

# Media processing
easy-thumbnails>=2.10

# Rich text & sanitization
bleach>=6.1

# HTTP client (Turnstile verification, geocoding)
httpx>=0.28.1

# HTTP client (Turnstile verification)
httpx>=0.27
